import 'package:flutter/material.dart';

class Stuff{
  String Stuffname;
  Stuff(this.Stuffname, );
}

class TradeSfBox extends StatelessWidget {
  const TradeSfBox({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
